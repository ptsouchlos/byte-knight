//! Test-only crate. Houses cross-cutting integration tests in `tests/`.
//! Intentionally empty — exists to give the integration tests a home that
//! does not belong to any single production crate.
